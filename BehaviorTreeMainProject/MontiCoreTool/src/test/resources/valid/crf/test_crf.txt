Parameter beam : Element {
    length: Double
}

Parameter robot : Agent {
    type: String,
    speed: Double
}

Parameter plate : Element {
    thickness: Double,
    material: String
}

Parameter firstLocation : Location {
    xcoordinate: Double,
    ycoordinate: Double
}

Parameter positionOnRail : Location {
    position: Double,
    direction: String
}

// Predicate definition

predicate isAt {
    agent - Agent,
    location - Location
}

predicate hasTool {
    agent - Agent,
    tool - Tool
}

// Action definition 

Action pickUp {
    name = "pickup"   
    parameters {
        obj : beam
        rob : robot
        loc : firstLocation}   
    preconditions {
        at(obj = object loc = location)
        atAgent(robot = agent loc = location)
        hasTool(robot = agent robot.tool = tool) }    
    effects {
        holding(robot = agent obj = object)
        at(robot = agent loc = location) }   
    function {{obj,robot},{Boolean}}
    implementation {
        pickupImplementation }
}



