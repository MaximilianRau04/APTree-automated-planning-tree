Parameter beam : Element {
    length: Double
}

Parameter vacuumGripper : Tool {
    isOn: Boolean
}

Parameter robot : Agent {
    type: String,
    speed: Double
}

Parameter plate : Element {
    thickness: Double,
    material: String
}

Parameter firstLocation : Location {
    xcoordinate: Double,
    ycoordinate: Double
}

Parameter positionOnRail : Location {
    position: Double,
    direction: String
}

// parameter instance

ParameterInstance: beam {b1}
ParameterInstance: beam {b2}
ParameterInstance: robot {r1}

// Predicate definition

predicate isAt {
    object - Element,
    location - Location
}


predicate atAgent {
    agent - Agent,
    location - Location
}

predicate hasTool {
    agent - Agent,
    tool - Tool
}

// Action definition 

Action pickUp {
    name = "pickup"   
    parameters {
        obj : beam
        rob : robot
        loc : firstLocation   
        tool : vacuumGripper}
    preconditions {
        at(obj = object loc = location)
        atAgent(robot = agent loc = location)
        hasTool(agent = robot tool = tool) }    
    effects {
        holding(robot = agent obj = object)
        at(robot = agent loc = location) }   
    function {{obj,robot},{Boolean}}
    implementation {
        pickupImplementation }
}



